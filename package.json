{
  "name": "protoforge-ai-builder",
  "version": "2.0.0",
  "private": true,
  "description": "Turn an uploaded image or document plus an instruction into a working web prototype.",
  "type": "module",
  "scripts": {
    "check": "node --check api/health.js && node --check api/analyse.js",
    "dev": "vercel dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ai": "^6.0.0",
    "zod": "^4.0.0"
  }
}
