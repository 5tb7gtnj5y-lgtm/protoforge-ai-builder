{
  "name": "protoforge-ai-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Turn documents, notes, and images into editable working web prototypes.",
  "type": "module",
  "scripts": {
    "check": "node --check api/health.js && node --check api/analyse.js",
    "dev": "vercel dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ai": "^6.0.0",
    "zod": "^4.0.0"
  }
}
